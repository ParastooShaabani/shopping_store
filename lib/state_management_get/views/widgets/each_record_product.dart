import 'package:flutter/material.dart';

class EachProductRecord extends StatelessWidget {
  const EachProductRecord({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
